# ClipLink Android

React Native/Expo companion app for ClipLink. The current screen validates a Windows pairing payload and provides a clipboard test action. QR camera scanning and the encrypted local transport are the next implementation steps.

Run with `npm install` followed by `npm start` when the Expo toolchain is available.
